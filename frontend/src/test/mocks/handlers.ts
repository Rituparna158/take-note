import type { HttpHandler } from "msw";

// No API handlers are defined yet — AB-1001 introduces no feature endpoints.
// Feature tickets add their MSW handlers here as they implement each API call.
export const handlers: HttpHandler[] = [];
